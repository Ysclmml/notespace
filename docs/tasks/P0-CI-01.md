# P0-CI-01 — 固定工具链的持续集成门禁

- Status: CLAIMED
- Owner / next owner: Integration/QA (`/root`) / Integration review
- Base revision / head revision: `576a435` / working tree
- Requirement IDs: `OPS-CI-001`, `OPS-BUILD-001`, `OPS-CONTEXT-001`
- Product UX IDs: 纯基础设施任务，不适用
- Test / acceptance IDs: `CI-001`, `BUILD-001`, `PROC-001`
- ADRs / contract and schema versions: `ADR-0001`; IPC v1 尚由并行 `P0-CONTRACT-01` 冻结
- Feature flags: 无
- Owned and touched paths: `.github/workflows/**`; 本任务专属 repository hygiene validator；本 task note；`PROJECT_STATE.md` 的 `P0-CI-01` 行

## Goal and non-goals

目标：在权限最小、工具链和外部 Action 均可审计的 CI 中，从干净 checkout 执行文档门禁、格式、lint、类型、前后端测试、契约入口、安全/依赖扫描和 Tauri 调试产物构建；任一步失败即阻止合并，并上传可定位的 macOS 调试应用 artifact。

非目标：不实现或重解释 IPC 契约，不实现产品功能，不建立发布签名/公证，不把未来 nightly、E2E、故障注入或正式多平台发布冒充为本任务已交付。

## Dependencies and baseline

- Dependency task/freeze status: `P0-BOOT-01` 已在 `main` 完成；`P0-CONTRACT-01` 并行执行，CI 通过统一本地门禁入口自动接入其后续契约检查。
- Baseline commands: `pnpm install --frozen-lockfile`; `ruby scripts/validate_design_docs.rb`; `pnpm verify`（Rustup toolchain bin 已加入进程 `PATH`）。
- Baseline result: base `576a435` PASS；21 个 Markdown、56 requirements、83 tests；3/3 frontend tests、Rust fmt/clippy/tests、Vite build 和 Tauri debug build 均通过。

## Acceptance criteria status

| Requirement / acceptance ID | Expected evidence | Status |
|---|---|---|
| `OPS-CI-001` / `CI-001` | 干净 hosted runner 执行规范门禁，失败停止，构建 artifact 可定位 | IN PROGRESS |
| `OPS-BUILD-001` / `BUILD-001` | CI 使用仓库锁定的 Node/pnpm/Rust 和 frozen lockfiles 完成桌面构建 | IN PROGRESS |
| `OPS-CONTEXT-001` / `PROC-001` | 工作流、权限、版本和后续契约接入点无需聊天即可理解 | IN PROGRESS |

## Changes made

- 已完成独立 worktree 冷基线，尚未修改实现。

## Decisions and assumptions

- CI primary runner 与 macOS 首发方向一致；正式全平台矩阵和签名/公证仍属于后续阶段。
- GitHub Actions 必须固定到完整 commit SHA，并用注释记录对应 release tag；workflow token 仅授予 `contents: read`。
- Node 从 `.node-version` 读取，pnpm 使用 `package.json` 的精确版本，Rust 由 `rust-toolchain.toml` 固定；不得使用漂移的 `latest` 工具版本。
- 本任务只接线统一 `pnpm verify`；契约 owner 应把 schema drift/contract gate 纳入该入口，CI 不复制第二套语义。

## Verification evidence

| Test / acceptance ID | Exact command and environment | Result | Artifact or failure |
|---|---|---|---|
| `PROC-001` baseline | `ruby scripts/validate_design_docs.rb`; macOS arm64; base `576a435` | PASS | snapshot `e0905a48...` |
| `BUILD-001` baseline | `pnpm install --frozen-lockfile` then `pnpm verify`; Node 24.14.0, pnpm 10.32.1, Rust 1.98.0 | PASS | Tauri debug binary built; no user data |

## Open questions and blockers

- GitHub remote 尚未配置，因此本地只能校验 workflow 结构与等价命令，无法伪报 hosted run。Owner: repository maintainer；affected: `CI-001` 的 hosted evidence。安全工作可继续，首次 push 后必须以真实 run URL/artifact 补证。

## Remaining numbered steps

1. 增加 SHA-pinned、最小权限的 macOS CI workflow。
2. 增加 repository hygiene 检查，并接入文档、依赖与统一 build/test gate。
3. 本地验证 workflow 结构、失败边界、完整 `pnpm verify` 和 debug app bundle。
4. 更新为 REVIEW、提交并交给 Integration；remote 建立后补 hosted run 证据。

## Data safety, recovery, and temporary artifacts

- CI 不读取、上传或缓存用户文档、剪贴板、恢复数据或真实语料；只处理仓库内合成源与构建产物。
- artifact 不含签名密钥；token 为只读。依赖缓存不保存 `node_modules`，构建输出有短期 retention。

## Single recommended next action

实现并验证 `.github/workflows/quality.yml` 的固定工具链、最小权限质量门禁和 macOS debug artifact。
