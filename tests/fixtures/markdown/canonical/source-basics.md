# Canonical source

Plain text with **strong**, *emphasis*, ~~strike~~, and an escaped \*asterisk\*.

> A quoted line
>
> - nested item

1. ordered item
2. second item

```ts
const value = "fenced code stays byte-for-byte";
```

<sample-element data-mode="source-only">raw HTML remains source</sample-element>
