# Unknown syntax boundary

Text before the future extension.

:::future-panel mode="unrecognized"
keep **every** byte, including    spacing
and `inline | tokens`.
:::

Text after the future extension may be edited independently.
