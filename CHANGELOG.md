# 更新记录

## 0.2.1 — 2026-09-04

- 新增 Android 移动阅读端：与电脑处于同一局域网时，可自动发现或按主机地址连接桌面 NoteSpace，浏览目录、搜索并只读 Markdown；桌面共享默认关闭，仅共享明确勾选且已经保存的工作区，同一服务可供多个手机使用。
- 桌面与手机统一默认端口 `49920`，桌面可修改；手机只输入 IP 或主机名时自动补端口。移除占用界面的 Debug/无认证说明卡片，连接仍保留清晰的服务状态和失败提示。
- 手机支持按工作区保存完整 Markdown 离线副本，断网后继续浏览、搜索和从最近记录进入；下次连接同一电脑时自动原子刷新，失败保留上一份完整副本。
- 桌面、手机阅读、可分享 HTML/PDF 统一支持 `$...$`、`$$...$$`、`\(...\)` 与 `\[...\]`；代码、链接和未闭合分隔符保持字面量，无效公式局部回退。
- Android ARM64 可安装 Debug 包默认省略 Rust DWARF 调试信息，仍保留应用/WebView 调试、日志与局域网能力；本轮实测由约 141 MiB 降至约 32 MiB。原生调试入口继续保留完整符号。

手机应用自动更新仍未接入；本版不包含静默下载、安装或热更新。

## 0.2.0 — 2026-09-04

- 搜索结果命中词高亮，支持 Unicode 与大小写设置。
- HTML 分享文件嵌入图片与静态 Mermaid；新增 macOS 原生分页 PDF 导出，联网图片逐次授权，资源失败不生成缺图文件。
- 可视/源码统一格式快捷键，设置中支持搜索、录入、冲突检查、清除和恢复默认。
- 当前页查找替换、文件收藏、专注模式和会议/周报/技术方案模板。
- 收藏改为文件侧栏的紧凑折叠分组，支持文件右键收藏和失效标记/重试；可在设置中显示/隐藏，右键标题后需明确选择「关闭收藏」，关闭不会清空收藏，关闭工作区后按独立文件打开。
- 全文搜索改为独立弹窗和更清晰的应用自绘范围控件，默认搜索全部已打开工作区正文，可限制单根；正文可选正则，文件名/路径有独立正则筛选，文件名快速打开保持独立。最近记录仅紧凑显示查询文字，默认保留 15 条且可在设置中调整为 1–30 条；点击结果后重开搜索会恢复本次运行内的结果、滚动和最后点击项，不重复扫描。
- 打包应用注册 `.md/.markdown` 编辑关联，可由系统「打开方式」以前台标签打开；不静默接管系统默认应用，也不隐式打开父目录工作区。
- 替换正式应用图标，并同步 macOS、Windows 和各桌面尺寸资源。
- 新增双语离线使用帮助与本地 Markdown 模板库；帮助和关于分离，导出统一为 HTML/PDF 二级菜单。
- 新增固定 GitHub Releases 来源的版本检查：About 显示当前版本并可手动检查，启动检查可关闭、跳过只作用于指定版本；不自动下载/安装，不上传文档或路径。
- macOS 应用包补充系统编辑/窗口菜单本地化资源；About 的更新状态使用固定区域，检查过程和长提示不再改变弹窗外框高度。

macOS Apple Silicon 预览版，使用 ad-hoc 签名，尚未完成 Apple 公证。

## 0.1.1 — 2026-09-03

- 新增工作区全文搜索：多个根目录、大小写选项、行号与上下文，以及当前分屏中的结果定位。
- 新增静态 HTML 导出：基于最新 Markdown 正文生成阅读页面，支持表格、列表、代码和资源引用，不改变文档保存状态。
- 新增浏览恢复提示：查看暂不可用路径、重试或选择文件夹，支持只移除最近记录。
- 修复恢复浏览后的首次跨文件跳转缺少后退起点，以及搜索结果的可视位置映射与异步焦点保护。
- 保留既有标签、分屏、前进后退、保存和图片工作流，以及原有设置和浏览记录格式。

搜索只读取磁盘文本，不包含未保存编辑；达到扫描限制时明确提示。HTML 不打包图片，Mermaid 保留源码；单篇正文限 8 MiB，source-only 模式暂不导出 HTML。

macOS Apple Silicon 预览版，使用 ad-hoc 签名，尚未完成 Apple 公证。
