{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimum capability for the Markdown Workspace application shell",
  "windows": ["main"],
  "permissions": ["core:default"]
}
