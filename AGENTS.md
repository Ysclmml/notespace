# Repository Agent Rules

这些规则用于人类和 AI 协作者。目标是让上下文压缩或多人并行后，仍能继续实现同一个简单、可运行的本地 Markdown 编辑器。

## 开始前阅读

按顺序完整阅读：

1. `AGENTS.md`
2. 若本机存在 `internal-docs/PROJECT_STATE.md`，读取本地交接状态；全新克隆可直接继续下一项
3. `docs/DESIGN.md`
4. `docs/REQUIREMENTS.md`
5. 当前工作涉及的 accepted ADR；其中 `ADR-0005` 是精简边界，`ADR-0006` 固定可视/源码双表面，`ADR-0007` 固定本地文件、多工作区和一个右侧预览栏，`ADR-0008` 固定保存策略、多根文件树、工作区文件动作和可视表格工具，`ADR-0009` 固定可恢复删除与可靠 dirty 关闭，`ADR-0010` 固定工作区右键入口与单层目录创建，`ADR-0011` 固定横向编辑分组、临时标签与图片链接预览
6. 导航工作还须阅读 `ADR-0012`：Markdown 固定/临时标签跳转与窗口级跨标签前进/后退；`ADR-0013` 将代码右侧预览合并为编辑组、右分屏改为移动，并增加启动浏览恢复、当前页查找和递归隐藏项开关；`ADR-0014` 固定外部文件监听、干净重载与冲突保护；图片工作还须阅读 `ADR-0015`，固定每工作区截图位置、原生剪贴板兼容和本地图片单文件授权。历史探索仅在本机 `internal-docs/design/` 保留（如存在），不作为当前实现依据；冲突时以 `ADR-0005`～`ADR-0015` 与当前 baseline 为准

聊天记录不是项目状态。影响产品实现的稳定决定、限制写入公开设计文档或 ADR；个人维护、发布步骤、动态验证结果和下一步写入本地 `internal-docs/PROJECT_STATE.md`。`internal-docs/` 已被 Git 忽略，不得强制添加；公开文档和构建不得依赖其中的文件。

如果仓库存在 `.codegraph/`，理解代码时先用 CodeGraph；否则先用 `rg` / `rg --files`。

## 产品与技术边界

搜索、导出与恢复工作另读 `ADR-0016`；写作工具和分享导出另读 `ADR-0017`；收藏分组、全局搜索、离线帮助、本地模板库与导出子菜单另读 `ADR-0018`；自绘搜索范围、正文/路径正则、收藏显示设置与固定 GitHub 更新检查另读 `ADR-0019`；搜索历史、收藏关闭菜单和 Markdown 文件关联另读 `ADR-0020`；搜索会话恢复、可配置历史上限、macOS 系统菜单本地化和稳定更新状态区另读 `ADR-0021`，冲突时以最新适用 ADR 和 baseline 1.8 为准。搜索不包括未保存修改；搜索结果只在本次运行内保留，最近搜索只持久化默认 15、可配 1–30 条条件，选择历史只回填、不自动扫描。导出包括最新内存正文但不执行保存。HTML 打包图片与静态 Mermaid，macOS 原生分页 PDF；联网图片仅在每次导出显式勾选后读取，不后台抓取或上传。当前页替换可撤销；收藏只存路径，标题右键打开菜单，只有明确选择关闭才隐藏且不删除收藏。专注只改界面，模板生成普通 Markdown；自定义模板存当前应用用户数据目录而非安装包。更新检查只访问 `https://api.github.com/repos/Ysclmml/notespace/releases/latest`，可关闭启动检查且不自动下载/安装，不接受任意更新源或上传文档/路径。打包应用只声明 `.md/.markdown` 编辑关联，系统默认归用户决定；关联路径走有界队列和既有文档预检，不传正文。不引入持久索引、跨文件批改或重命名修复。

- 这是普通的单用户本地文本编辑器，不是多租户服务或高安全文件网关。
- 应用运行时使用 React、TypeScript、Milkdown/ProseMirror 可视编辑、CodeMirror 6 显式源码编辑、Tauri 2 和 Rust；Node 用于构建、测试与轻量检查。不要引入 Ruby。
- Markdown 文件是 Markdown 正文的唯一持久化真相；普通代码/文本文件以各自磁盘文本为真相。渲染、Outline、索引和预览都是可重建投影。
- `DocumentSession`、`Tab` 和 `HistoryEntry` 是不同概念：正文可共享，文档引用与阅读位置按 Tab 隔离；顶部前进/后退使用窗口级有界访问轨迹，可跨 Tab/组恢复，不复制或持久化正文。
- 只给当前实际实现的 Tauri 命令定义类型。禁止恢复预冻结的 37 命令、巨型生成 schema、通用 feature flag 框架或可信宿主实验。
- 不新增服务端、账户、遥测或网络上传。除 ADR-0017 的逐次导出授权和 ADR-0019 的固定 GitHub 更新检查外，任何网络能力都必须由用户提出并单独决策。

## 仅保留三项实用护栏

1. **Base64 防卡死**：大段 `data:image/...;base64,` 粘贴必须在可视或源码编辑事务前拒绝；文件先由 Rust 固定缓冲预检，命中病态 data URI/长行时不得把正文送入 JS。
2. **截图落盘**：图片默认先写入 Markdown 所在目录，也可由每个工作区指定现存目录；成功后前端才插入相对链接，跨卷使用文件 URI。未保存文档先 Save As；Undo 只撤销链接，不删除图片。图片数据不进入 IPC 或 Markdown Base64。
3. **原子保存**：同目录临时文件写入、flush 后 rename；失败保留旧文件。用轻量磁盘版本做外部修改检查，不引入文件锁或通用同步系统。

不要把以上护栏扩展成 quarantine、HMAC/nonce、repair token、资产 journal、通用崩溃恢复引擎或巨型 IPC 压测，除非新的真实需求和 ADR 明确要求。

## 实现方式

- 优先完成可运行的纵向切片：界面入口 → 状态模型 → Tauri 命令 → 磁盘结果 → 测试。
- 前端功能按 `src/features/<domain>/` 放置；共享应用状态位于 `src/app/`；小型 IPC adapter 位于 `src/infrastructure/tauri/`。
- Rust 命令保持薄；文件预检、保存、资产写入等逻辑放在可单测模块中。
- `normal` 文档默认进入稳定的可视化编辑；标题、列表、链接和表格不得因光标进入就自动切回 Markdown 源码。只有用户明确选择“源码”或文档被预检为 `sourceOnly` 时使用 CodeMirror。
- 可视与源码两种编辑表面都必须保持 IME、选择和 Undo/Redo。未编辑文档保存必须零差异；用户首次可视化修改后，允许 Milkdown serializer 规范化该篇 Markdown。
- 常见 UTF-8 代码/文本文件在 Tab 中使用 CodeMirror 编辑和原子保存，不进入 Milkdown；浮层保持只读，“在右侧打开”进入正常可编辑分组，不再额外创建辅助栏。
- 一个窗口可保持多个工作区，文件侧栏同时显示全部已打开根及各自文件树；当前文档以最长匹配根为归属，展开并突出显示。最近工作区/文件只保存路径。
- 编辑区支持扁平横向分组：默认只有左侧一个活动组，各组有独立 Tab；右键向右分屏移动原 Tab，可保留空左组，支持跨组拖动、分隔线调整。文件树单击打开该组的斜体临时 Tab，双击或编辑固定；新文件跟随当前聚焦组。代码右侧打开复用右邻组，右most 来源复用所在组并保留源页，不额外新增第三辅助栏；不建设纵向/递归 Pane 或 IDE docking 框架。
- 启动默认恢复上次浏览，可设置空白启动。只保存有界工作区/文件路径、分组/标签、固定状态和数值阅读位置；不保存正文、未命名文档、Undo 或前进后退轨迹。恢复时重新读取磁盘，迟到结果不能覆盖用户新操作。
- 当前页查找 `Cmd/Ctrl+F` 只搜索活动编辑页，可视、源码和代码共用入口；匹配装饰不写正文、不 dirty、不进 Undo。工作区隐藏文件/文件夹默认不显示，每根菜单可独立开启并递归应用；仍排除重目录、符号链接和不支持文件类型。
- 默认手动保存并在关闭 dirty session 时提示；可选 1–300 秒停止输入后自动保存已有路径文档。未命名文档不自动 Save As，失败保持 dirty。
- 原生文件通知刷新相关工作区，聚焦和 30 秒检查兜底；先比较元数据，干净文件变化才预检重载。dirty、缺失、不可读或 blocked 保留内存并暂停普通/自动保存，重载或覆盖须遵循明确确认；缺失不隐式重建。保存以磁盘版本为前提且保持原子写入，不承诺锁或原子 compare-and-swap。迟到读取/保存须保留原 Tab 引用所有权，不能污染关闭后重开的同名文件。
- 工作区根/现有子目录可新建受支持的 UTF-8 Markdown/代码/文本或单层空目录且不覆盖；通过根/目录/文件同级与空白区域右键操作，不保留常驻根工具栏。根、目录和文件可在系统文件管理器中显示并复制路径。文件/目录只允许在明确确认后移入系统废纸篓，Rust 必须拒绝工作区根和根外目标；不要借此扩展永久删除、移动、重命名或通用文件 API。
- GFM 表格保持真可视；宽表格局部横向滚动，列宽拖动只属于 view、不写 Markdown；网格插入、行列操作和普通结构右键进入正常可撤销正文事务。
- 内部 Markdown 链接（含可视 hover 卡片）统一进入应用导航，不使用 `window.location`；显式 Markdown 路径不依赖文件树收录。普通跨文档点击在未固定 Tab 原位替换，已固定/编辑 Tab 则新建预览页；同文档 anchor 留在原页。修饰键/中键可开新 Tab，代码/图片仍专用预览。关闭/删除清理窗口轨迹，不复活已关闭 Tab 或已放弃正文。
- Mermaid 和图片查看器只消费渲染结果，不修改正文；图片链接无需行号，点击后进入缩放/平移查看器，加载失败可关闭返回。图片链接查看器的远程请求只响应用户点击，不后台预抓取或上传；文内嵌入图片保持既有加载链路。
- 依赖应当由当前功能证明必要。不要为可能的未来扩展提前建设框架。

## 修改与协作

- 先检查工作树，保留用户和其他代理的无关修改。
- 使用 `apply_patch` 编辑文件；不要做无关格式化或依赖升级。
- 多日、跨模块或需要他人接手的工作才创建本地 `internal-docs/tasks/<TASK-ID>.md`。小而完整的改动无需为流程制造文档。
- 并行任务应声明独占路径；根清单、lockfile、全局配置和 `PROJECT_STATE.md` 由集成者合并。
- Accepted ADR 不改写；用新 ADR 取代。旧提交与历史任务不代表当前需求。
- 永远不要读取后提交真实用户文档、剪贴板内容、个人绝对路径、密钥或大型 Base64 夹具。测试数据在临时目录运行时生成。

## 风险相称的验证

每次交付至少运行受影响层的格式、类型、单测和构建；不要为了仪式运行与改动无关的攻击、取消、陈旧 revision 或宿主人工门禁。

- 编辑器：覆盖可视/源码显式切换、可编辑 GFM 表格、CJK composition、Undo/Redo、链接导航、初始化零修改与编辑后 Markdown 序列化。
- 导航：覆盖 Markdown 固定/临时策略、真实 hover/正文/源码链接、独立文件与跨根解析、跨 Tab/组前进后退、位置恢复、forward 截断与迟到读取；共享正文、dirty 关闭、Save As 迁移和关闭/删除后轨迹过滤。
- 文件：覆盖预检分类、普通大文档、代码/文本新建与 Save As 身份迁移、工作区内 create-new 不覆盖、跨平台 reveal、复制路径、受控废纸篓边界和原子保存故障。
- 工作区：覆盖多根文件树同时显示、独立折叠、活动根、右键关闭、最近路径恢复、独立文件和失效路径降级；隐藏项递归过滤、每根偏好及迟到刷新不覆盖新偏好。
- 保存：覆盖 manual 默认、1–300 秒归一化、inactivity 重排、untitled 跳过、迟到正文校验和失败仍 dirty。
- 外部变化：覆盖临时目录真实新增/修改/删除和原子替换、事件合并/清理/兜底、干净重载、dirty 冲突与删除后保留；保存前版本拒绝、确认/取消，以及本地编辑/保存/关闭重开后的迟到保护。
- 表格：覆盖真可视单元格、局部横向滚动、view-only 列宽不 dirty、尺寸网格、行列操作、Undo/Redo 与序列化。
- 分栏：覆盖只读浮层/当前组/右组代码去向、真实行号、滚动与复制；右移不复制、右组复用及迟到关闭保护；横向多组的临时替换/固定、焦点路由、跨组拖动、独立视图/共享正文和最后 dirty 引用关闭。
- 浏览恢复与查找：覆盖 empty/restore、从磁盘重读且无草稿、显式空组、缺失文件与迟到恢复；查找 CJK/计数/循环/Esc、各表面与分屏隔离、编辑和 Undo 不受影响。
- 资产：覆盖写入成功/失败与“成功后才插链接”。
- Mermaid：覆盖超时/失败回退与查看器缩放交互。
- 搜索与更新：覆盖正文/路径正则、自绘范围控件、既有扫描预算和未保存正文排除；结果跳转后重开恢复本次运行内的结果、滚动与最后激活项且不重复扫描，历史默认 15、可配 1–30 并立即裁剪；固定 GitHub 端点、启动开关、单版本跳过、手动检查、稳定 About 状态区、用户手势打开发布页及失败时本地功能不受影响。

标准本地门禁为 `pnpm verify`。Hosted CI 在存在远程仓库和发布需要时再启用，不阻塞本地产品实现。

## 交付状态

完成或上下文即将压缩时，更新本地 `internal-docs/PROJECT_STATE.md`（不存在时可创建）：说明当前可运行能力、未完成部分、准确验证命令和唯一下一步。内部交接不提交到 Git；涉及产品规则的变更仍同步公开设计或新 ADR。不要把“设计过”“曾有实验代码”写成当前已实现能力。
